
    let inputWidth;
    let inputHeight;
     inputWidth=prompt("Nhập Width");
     inputHeight=prompt("Nhâp Height");
    let Width=parseInt(inputWidth);
    let Height=parseInt(inputHeight);
let area=Width*Height;
document.write("Area="+area)